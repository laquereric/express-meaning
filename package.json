{
  "name": "express-meaning",
  "version": "0.1.0",
  "private": true,
  "description": "A TODO list that reads Meanings from magenticmarket.ai over CPCP, and composes both into a prompt you paste into Chrome AI Mode.",
  "type": "commonjs",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test test/"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
